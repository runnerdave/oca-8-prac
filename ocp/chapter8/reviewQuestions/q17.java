<div class="answerContent"><p class="AnswerTxt"><b> Answer: A;B;D;G </b></p><div class="ansText"><span class="dCode dNoWrap">ObjectOutputStream</span> and <span class="dCode dNoWrap">ObjectInputStream</span> perform serialization and deserialization on a low-level stream, respectively, so A and G are correct. <span class="dCode dNoWrap">PrintStream</span> and <span class="dCode dNoWrap">PrintWriter</span> format text for a low-level <span class="dCode dNoWrap">OutputStream</span> and <span class="dCode dNoWrap">Writer</span>, respectively, so B and D are also correct. <span class="dCode dNoWrap">FileWriter</span> and <span class="dCode dNoWrap">FileInputStream</span> operate on a file directly and are low-level streams, so C and F are incorrect. Finally, <span class="dCode dNoWrap">OutputStream</span> is an abstract parent class and is neither high-level nor low-level, so E is incorrect.</div></div>